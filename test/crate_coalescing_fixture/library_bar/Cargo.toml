[package]
name = "library-bar"
version = "0.1.0"
edition = "2024"

[lib]

[dependencies]
http = "=1.3.1"
local-helper = { path = "local-helper" }
log = "=0.4.29"
serde = { version = "=1.0.228", default-features = false, features = ["derive"] }
